interface:
  display_name: "1st"
  short_description: "Repo-specific patterns and workflows for 1st"
  default_prompt: "Use the 1st repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true